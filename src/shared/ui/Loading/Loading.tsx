import styles from './Loading.module.scss'

const Loading = () => {
  return (
    <div className={`container ${styles.wrapper}`}>
      <h3>Загрузка...</h3>
    </div>
  )
}

export default Loading